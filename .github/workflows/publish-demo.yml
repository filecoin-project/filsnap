name: Publish Demo
env:
  CI: true
  FORCE_COLOR: 1
on: [push, pull_request]
jobs:
  publish:
    runs-on: ubuntu-latest
    if: github.ref_name == 'master'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2.2.4
        with:
          version: 8
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm -r --filter demo run build
      - uses: fission-suite/publish-action@v1.2.7
        with:
          machine_key: ${{ secrets.FISSION_MACHINE_KEY }}
          workdir: 'examples/demo'
  preview:
    runs-on: ubuntu-latest
    if: github.ref_name != 'master'
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2.2.4
        with:
          version: 8
      - uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'pnpm'
      - run: pnpm install
      - run: pnpm -r --filter demo run build
      - name: Add report to web3.storage
        uses: web3-storage/add-to-web3@v2
        id: add_to_web3_storage
        with:
          path_to_add: packages/examples/demo/dist
          web3_token: ${{ secrets.WEB3_TOKEN }}
      - name: Add url to GITHUB_STEP_SUMMARY
        run: |
          echo "
          ### Preview URL
            Check app preview: https://w3s.link/ipfs/${{ steps.add_to_web3_storage.outputs.cid }}
            URL: ${{ steps.add_to_web3_storage.outputs.url }}
          " >> $GITHUB_STEP_SUMMARY
